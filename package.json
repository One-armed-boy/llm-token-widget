{
  "name": "llm-token-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "verify": "node scripts/docs-check.mjs && npm test",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "docs-check": "node scripts/docs-check.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
